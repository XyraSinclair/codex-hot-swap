#!/usr/bin/env bash
set -euo pipefail

if [ "${FAKE_CODEX_AUTH_FAIL_IF_CALLED:-0}" = "1" ]; then
  echo "codex-auth should not have been called" >&2
  exit 99
fi

if [ "${FAKE_CODEX_AUTH_LOG:-}" != "" ]; then
  printf '%s\n' "$*" >> "$FAKE_CODEX_AUTH_LOG"
fi

exit 0
